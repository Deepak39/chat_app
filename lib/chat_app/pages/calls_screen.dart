import "package:flutter/material.dart";

class CallsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("This is The calls Screen.",style: new TextStyle(color: Colors.black),),
    );

  }
}