import "package:flutter/material.dart";

class StatusScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: new Text("This is Status Screen",style: new TextStyle(color: Colors.black),),
    );
  }
}